all:
  hosts:
    # has a teleport address -> ansible_host becomes it
    p1:
      ip: 10.0.0.1
      teleport: p1.teleport.example.com
    # no teleport -> ansible_host falls back to the existing ansible_host
    p2:
      ip: 10.0.0.2
      ansible_host: 10.9.9.2
